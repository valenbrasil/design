# Separator

Linha divisória de 1px. `orientation` controla horizontal (padrão) ou vertical. Se `label` for passado, ignora `orientation` e mostra um divisor com texto centralizado entre duas linhas (ex. "ou").

```jsx
<Separator label="ou" />
```
