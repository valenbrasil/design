# Tag

Etiqueta com fundo `surface-subtle` e borda sutil, para valores selecionáveis (ex. filtros, tags de conteúdo). Quando `onRemove` é passado, mostra um botão "×" (ícone `x`) que a remove.

```jsx
<Tag onRemove={() => removerFiltro("Itajaí")}>Itajaí</Tag>
```
