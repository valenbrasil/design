# Progress

Barra de progresso linear (0–100, valores fora do intervalo são recortados). `label` e/ou `showValue` (mostra "N%") aparecem acima da barra quando presentes. Tamanhos `sm`/`md`(padrão)/`lg` mudam apenas a altura.

```jsx
<Progress value={62} label="Documentação" showValue />
```
