# Skeleton

Placeholder de carregamento com efeito de brilho (shimmer) animado. `width`/`height` aceitam número (px) ou string CSS; `radius` é qualquer token de raio (padrão `--radius-sm`).

```jsx
<Skeleton width={180} height={16} />
```
