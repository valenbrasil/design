# NavItem

Item de navegação lateral (sidebar do dashboard), com ícone, rótulo e um contador opcional (`badge`) alinhado à direita. `active` usa `--sidebar-accent`/`--sidebar-accent-foreground` em vez do hover cinza padrão.

```jsx
<NavItem icon="layout-dashboard" label="Visão geral" active />
```
