# Breadcrumb

Trilha de navegação hierárquica separada por `chevron-right`. O último item da lista é sempre texto simples (nunca um link), mesmo que tenha `href`.

```jsx
<Breadcrumb items={[{ label: "Laudos", href: "/laudos" }, { label: "VB-2026-114" }]} />
```
