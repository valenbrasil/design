# Textarea

Campo de texto multilinha com os mesmos estados visuais do Input (foco, `invalid`, `disabled`). `rows` define a altura inicial (padrão 4); é redimensionável verticalmente pelo usuário.

```jsx
<Textarea rows={4} placeholder="Observações da vistoria" />
```
