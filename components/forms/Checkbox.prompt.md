# Checkbox

Caixa de seleção controlada (`checked` + `onChange`) ou não controlada (`defaultChecked`). Mostra um ícone `check` quando marcada; fundo e borda usam `--primary`.

```jsx
<Checkbox label="Manter conectado" defaultChecked />
```
