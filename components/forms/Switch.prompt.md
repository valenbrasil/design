# Switch

Alternador on/off para preferências binárias (equivalente semântico ao Checkbox, visual de toggle). Controlado via `checked`+`onChange` ou não controlado via `defaultChecked`.

```jsx
<Switch label="Notificar por e-mail" defaultChecked />
```
